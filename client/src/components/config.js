//With this setup, if I ever need to change the base URL for my API,
//I only have to update it in config.js.

const config = {
    apiBaseURL: "http://localhost:3001"
  };
  
  export default config;